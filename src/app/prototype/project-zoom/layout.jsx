import "dialkit/styles.css";

export const metadata = {
  title: "Project Zoom Prototype",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ProjectZoomPrototypeLayout({ children }) {
  return children;
}
